package Observables
import public initlater Subject

// ============================================================================
public interface IObserver<T>
  function onError(string error)
  function onCompleted()
  function onNext(T element)

// ============================================================================
public interface IObservable<T>
  function subscribe(IObserver<T> observer)